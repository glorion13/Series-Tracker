<phone:PhoneApplicationPage
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit" 
    x:Class="SeriesTracker.SeriesDetails" 
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="800"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    shell:SystemTray.IsVisible="False"
	>
	<phone:PhoneApplicationPage.Resources>
		<DataTemplate x:Key="TitleTemplate">
			<Grid Margin="-10,76,0,-76">
				<Rectangle Height="130" Opacity="0.2" Width="900"/>
				<TextBlock Text="{Binding Title}" Style="{StaticResource PhoneTextTitle3Style}" Visibility="{Binding Banner, ConverterParameter=True, Converter={StaticResource VisibleIfNotNullOrEmpty}}" Opacity="0.85" VerticalAlignment="Top" />
			</Grid>
		</DataTemplate>
		<DataTemplate x:Key="HeaderTemplate">
			<Grid>
				<TextBlock Text="{Binding}" Opacity="0.6"/>
			</Grid>
		</DataTemplate>
		<DataTemplate x:Key="DataTemplate1">
				<Border Margin="{StaticResource PhoneTouchTargetOverhang}"  Padding="{StaticResource PhoneTouchTargetOverhang}" Width="92" Height="92" Background="{Binding GroupBackgroundBrush}">
					<TextBlock Text="{Binding Key}" Style="{StaticResource PhoneTextLargeStyle}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
				</Border>
		</DataTemplate>
	</phone:PhoneApplicationPage.Resources>

	<phone:PhoneApplicationPage.FontFamily>
		<StaticResource ResourceKey="PhoneFontFamilyNormal"/>
	</phone:PhoneApplicationPage.FontFamily>
	<phone:PhoneApplicationPage.FontSize>
		<StaticResource ResourceKey="PhoneFontSizeNormal"/>
	</phone:PhoneApplicationPage.FontSize>
	<phone:PhoneApplicationPage.Foreground>
		<StaticResource ResourceKey="PhoneForegroundBrush"/>
	</phone:PhoneApplicationPage.Foreground>
	<phone:PhoneApplicationPage.DataContext>
		<Binding Path="SeriesDetails" Source="{StaticResource Locator}"/>
	</phone:PhoneApplicationPage.DataContext>

    <!--LayoutRoot contains the root grid where all other page content is placed-->
    <Grid x:Name="LayoutRoot">
        <controls:Panorama HeaderTemplate="{StaticResource HeaderTemplate}"  Title="{Binding Series}"  TitleTemplate="{StaticResource TitleTemplate}" RenderTransformOrigin="0.5,0.5">
        	<controls:Panorama.Background>
        		<ImageBrush ImageSource="{Binding SeriesDetails.Series.Banner, Source={StaticResource Locator}}" AlignmentX="Left" AlignmentY="Top" Stretch="Uniform"/>
        	</controls:Panorama.Background>

            <!--Panorama item one-->
            <controls:PanoramaItem Header="details">
                <Grid/>
            </controls:PanoramaItem>

            <!--Panorama item two-->
            <controls:PanoramaItem Header="episodes">
                <Grid>
                	<toolkit:LongListSelector ItemsSource="{Binding Episodes}" ShowListFooter="False" ShowListHeader="False" GroupItemTemplate="{StaticResource DataTemplate1}">	
						<toolkit:LongListSelector.GroupItemsPanel>
							<ItemsPanelTemplate>
								<toolkit:WrapPanel/>
							</ItemsPanelTemplate>
						</toolkit:LongListSelector.GroupItemsPanel>
						<toolkit:LongListSelector.GroupHeaderTemplate>
							<DataTemplate>
								<Border Padding="{StaticResource PhoneTouchTargetOverhang}" HorizontalAlignment="Left" VerticalAlignment="Top" Width="64" Height="64" Margin="{StaticResource PhoneMargin}">
									<Border.Background>
										<SolidColorBrush Color="{StaticResource PhoneAccentColor}"/>
									</Border.Background><TextBlock Text="{Binding Key}" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center" /></Border>
							</DataTemplate>
						</toolkit:LongListSelector.GroupHeaderTemplate>
						<toolkit:LongListSelector.ItemTemplate>
							<DataTemplate>
								<Grid Margin="0,0,-28,12" Width="422">
									<Grid.RowDefinitions>
										<RowDefinition Height="Auto"/>
										<RowDefinition/>
									</Grid.RowDefinitions>
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="160"/>
										<ColumnDefinition/>
									</Grid.ColumnDefinitions>
									<Image Source="{Binding Image}" Height="90" Width="160" Stretch="UniformToFill" d:LayoutOverrides="GridBox" />
									<Grid VerticalAlignment="Top" Grid.Row="1">
										<Rectangle Fill="{StaticResource PhoneAccentBrush}" d:LayoutOverrides="GridBox"/>
										<TextBlock TextWrapping="Wrap" Text="{Binding Name}" d:LayoutOverrides="Width, Height" FontSize="18.667" Margin="{StaticResource PhoneMargin}"/>
									</Grid>
									<TextBlock TextWrapping="Wrap" Text="{Binding Description}" Grid.Column="1" Grid.RowSpan="2" Style="{StaticResource PhoneTextSmallStyle}" FontSize="16"/>
								</Grid>
							</DataTemplate>
					</toolkit:LongListSelector.ItemTemplate>
					</toolkit:LongListSelector>
                </Grid>
            </controls:PanoramaItem>
        </controls:Panorama>
    </Grid>

</phone:PhoneApplicationPage>